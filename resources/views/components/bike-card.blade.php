@props(['bike'])
<article class="overflow-hidden rounded-3xl border-2 border-ink bg-paper shadow-hard"><a href="{{ route('bikes.show',$bike) }}" class="group block"><div class="relative aspect-[4/3] overflow-hidden bg-forest">@if($bike->images->first())<img src="{{ asset('storage/'.$bike->images->first()->path) }}" alt="{{ $bike->images->first()->alt }}" class="h-full w-full object-cover transition duration-500 group-hover:scale-105">@else<div class="grid h-full place-items-center text-7xl">🚲</div>@endif<span class="absolute left-4 top-4 rounded-full px-3 py-1 text-xs font-black uppercase {{ $bike->status==='Til salgs'?'bg-sun text-ink':'bg-paper text-rust' }}">{{ $bike->status }}</span></div><div class="p-6"><p class="text-xs font-bold uppercase tracking-[.18em] text-rust">{{ $bike->type }}</p><h2 class="mt-2 font-display text-2xl uppercase">{{ $bike->name }}</h2><div class="mt-4 flex items-end justify-between gap-4"><p class="text-sm text-ink/65">Str. {{ $bike->size }} · {{ $bike->rider_height }}</p><strong class="whitespace-nowrap text-xl">{{ number_format($bike->price,0,',',' ') }} kr</strong></div></div></a></article>
